// Master main entry point - for sender interface
import React from "react";
import ReactDOM from "react-dom/client";
import '../index.css';
import MasterApp from "./MasterApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MasterApp />
  </React.StrictMode>
);

